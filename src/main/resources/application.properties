# MySQL datasource used by Spring Boot
spring.datasource.url=jdbc:mysql://localhost:3306/inventory_db?serverTimezone=UTC&useSSL=false
spring.datasource.username=root
spring.datasource.password=Shree@21
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# We are using plain JDBC so disable JPA DDL auto (if no JPA)
spring.jpa.hibernate.ddl-auto=none

# Optional: change server port if 8080 taken
 server.port=8080
# Gmail SMTP example
# SMTP (Gmail example)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=shree21052005@gmail.com
spring.mail.password=kqis sdgz mkia jmks
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

## From address (optional)
#spring.mail.from=rjshree2105@gmail.com

# Who should receive low-stock notifications
notification.recipient=shree21052005@gmail.com
# dev admin secret (change this before using in production)

app.admin.secret=1234
app.jwt.secret=this-is-a-very-secret-key-1234567890
app.jwt.expiration-ms=3600000
